# Role / sales inbox signatures — PREPARED, NOT APPLIED

These are rebuilt versions of the role-inbox signatures using brand-scoped images
from `photos/role/<brand>/`.

**They have NOT been pushed to Gmail.** The live role inboxes are still on their
original signatures.

Each signature references only its own brand's images — verified:
- brand folder matches the send-as domain
- filename matches the inbox name
- no flat `photos/<role>.png` references (those caused the Richmond-logo incident)
- no WiseStamp / gifyu / cloudfront / HubSpot / tracking-pixel references
